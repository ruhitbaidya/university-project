import express from "express";
// import { studentControler } from "./student.controler";

const router = express.Router();

// router.post("/create-student", studentControler.studentCreateControler);

export const studentRouter = router;
